# set PATH so it includes shared_bin if it exists
if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/shared_bin:$PATH"
fi

# Source the shared aliases file.
if [ -f ~/.aliases ]; then
    source ~/.aliases;
fi
