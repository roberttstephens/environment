RewriteEngine on
#Enable after entries are no longer accepted
#RewriteRule .* /bown/2012 [R=301]

 # Redirects the BOWN2011 to the home page if it's from AU
 # RewriteCond %{HTTP_HOST} ^(.*).popsci.com.au$ [NC]
 # RewriteRule ^.*$ http://%{HTTP_HOST}/ [R=301,L]
 # RewriteRule !\.(js|ico|gif|jpg|png|css)$ index.php

# Rewrite conditions for ZF
RewriteCond %{REQUEST_FILENAME} -s [OR]
RewriteCond %{REQUEST_FILENAME} -l [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^.*$ - [NC,L]
RewriteRule ^.*$ index.php [NC,L]
